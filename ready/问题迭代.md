请仔细理解并分析项目，完全弄明白分类流程。然后新增下面的功能：
## 支持基于商品标题来获取分类

### 步骤
1. 新增一个基于商品标题的分类接口
2. 基于当前的`VISION_SYSTEM_PROMPT`修改为支持商品标题分类的system prompt，命名为`PRODUCT_TITLE_CATEGORY_SYSTEM_PROMPT`和对应的user prompt，命名为`PRODUCT_TITLE_CATEGORY_USER_PROMPT`，注意和图片识别的prompt不一样，这里只需要获取到最终的分类，所以不需要返回title、description、brand_name等信息，只需要返回`top_level_category`即可。
3. 根据`top_level_category`去进行后续的分类，这部分和逻辑可以直接复用现有的
4. 最终返回json格式的数据，包含商品的最终分类

### 注意事项
1. 当基于商品标题获取分类时，如果分类失败了，则需要再调用图片识别接口，基于用户传的图片（`image_url`）进行分类，最终保证一定要返回`best_target_path`字段。

### 接口参数
入参：
- title: 商品标题（required）
- image_url: 商品图片（optional）
- language: 商品标题语言（optional，默认为日语 ja）

出参：
- best_target_path: 最佳分类路径（required）
- alternatives: 分类备选路径

### 测试界面更新
- 前端测试界面进行相应更新，支持新的接口测试

---
有时候请求接口时会报下面的错误：
"""
{"detail":"Failed to parse category LLM JSON."}
"""
请分析原因（我看到这个错误是在`app/service.py`的`_choose_categories`函数中抛出的），并且给出优化方案。比如对prompt进行优化？或者添加重试机制？请你给出可行的方案建议。同时，我想统计报错的次数，应该怎么统计？

---
请仔细分析并理解当前的项目，然后对商品描述（description）这部分的生成进行优化：
## 1. 需要修改对应的提示词，使商品描述（description）的生成符合指定要求，包含特定部分和特定字段的内容。
description的参考格式如下所示：
```
【商品详情】
◆ブランド： アップル（Apple）
◆商品名： Magic Keyboard（テンキー付き - JIS配列）
◆型番： A1843
◆対象： ユニセックス
◆カラー： シルバー / ホワイト
◆接続方式： Bluetooth / Lightningポート
◆サイズ： W约41.89cm x D约11.49cm x H约0.41–1.09cm
◆重量： 約390g
◆程度： B（使用感あり。やや傷・汚れあり）

【商品紹介 / 型号说明】
モデル番号：A1843 本製品は、デスクワークの効率を最大化する「テンキー付き」のApple純正ワイヤレスキーボードです。

フルサイズレイアウト： 数字入力に便利なテンキーに加え、ドキュメントナビゲーションコントロール（Page Up/Downなど）やフルサイズの矢印キーを搭載。Excel作業やプログラミングに最適です。

安定したタイピング： 各キーの下にあるシザー構造を改良し、キーの安定性が向上。より正確で快適なタイピング体験を提供します。

低重心デザイン： 薄型ながらも適度な重みがあり、デスク上での安定感が抜群です。

長寿命バッテリー： 内蔵の充電式バッテリーは、一回のフル充電で約1ヶ月以上の駆動が可能です。

【おすすめポイント / 推荐语】
✨ 事務作業やクリエイティブ作業の強い味方！ テンキー付きモデルなので、数字入力のスピードが格段に変わります。A1644（テンキーなし）では物足りない、プロフェッショナルな方にこそ使っていただきたい一台です。
✨ Apple純正ならではのシームレスな体験 Macとの相性は言うまでもなく最高です。スリムなデザインはデスクをスタイリッシュに彩り、どんなワークスペースにも馴染みます。

【検索用キーワード】
#Apple #アップル #MagicKeyboard #マジックキーボード #A1843 #MQ052J/A #テンキー付き #ワイヤレスキーボード #JIS配列 #日本語配列 #Mac周辺機器 #エクセル作業 #純正品 #中古品 #RDX
```
分为4个部分：
1. 商品详情：必须要包含下述字段：
◆ブランド：
◆商品名： 
◆型番：
◆対象：
◆カラー：
◆サイズ：
◆重量：
◆程度：
如果有不能确认的字段，则返回为空内容，但是必须要有这个字段
2. 商品介绍 / 型号说明
根据商品品牌和型号和相关信息，生成专业的商品介绍，包含商品的详细功能、特点、优势等。
3. 推荐语
用简短的语句，总结商品的优点和特点，让买家觉得这个商品非常值得购买。
4. 搜索关键词
根据商品的名称、品牌、型号、特点等，生成相关的搜索关键词，用于在搜索引擎中搜索商品。

## 2. 修改description的返回格式
将description的返回格式修改为json格式，需要包含上面4个部分的详细内容。

## 3. 注意事项
- 注意一定要确保返回的json格式是正确的，否则会导致应用测解析失败
- 注意要包含对应的换行符，方便解析展示

## 4. 测试界面更新
- 前端测试界面进行相应更新，支持新的description格式展示,看是否满足要求
---
这里的字段名都要是英文名称,同时商品详情又包含多个字段
```
"description": {
    "商品详情": "string",
    "商品介绍 / 型号说明": "string",
    "推荐语": "string",
    "搜索关键词": "string"
  },
```
---
请深入分析` /api/v1/mercari/image/analyze`这个接口的实现，然后进行以下修改：
1. 需要将`image`参数改为`image_list`参数，支持上传多张图片（正面、背面图片），对图片进行分析，提取更多信息，比如产品型号、品牌、颜色、尺寸、重量、成色等信息。
2. 更新API.md文档
---
参数名我已经修改为了image_list，现在我需要知道这个参数的详细说明，是一个文件列表？还是字符串的拼接？app端应该怎么传值？请输出到API文件中。
---
请深入理解获取最终分类id的链路，现在我想改变数据源，使用`/Users/youbo/Desktop/remote-jobs/image-recognize/data/rdx_category.csv`作为查询数据源，应该修改哪些地方？

---
好的。我想把之前的mercari数据源换成rakuten的数据源，同时返回的分类id为rakuten的分类id，同时一并返回对应的meru_id、rakuma_id、zenplus_id字段。请你给出一个修改方案，我确认后再进行执行。我希望改动的点能尽可能少，比如可以先把rdx_category.csv文件的数据转换为之前支持的格式，只是多了一些id字段，不过如果你能提出更好的方案就更好了。
---
好的。请按照这个方案进行修改，其中 
- best_meru_id
- best_rakuma_id
- best_zenplus_id
改为
- meru_id
- rakuma_id
- zenplus_id
---
请深入理解获取`brand_name`和`brand_id`的链路.现在我想改变数据源，使用`/Users/youbo/Desktop/remote-jobs/image-recognize/data/rdx_brand.csv`作为查询品牌的数据源，并且返回所有相关平台对应的id字段,而不是之前的一个。最终返回格式为
```
{
"brand_name": "string",
"brand_id_obj": {
  "rakuten_brand_id": "string",
  "yshop_brand_id": "string",
  "yauc_brand_id": "string",
  "meru_brand_id": "string",
  "ebay_brand_id": "string",
  "rakuma_brand_id": "string",
  "amazon_brand_id": "string",
  "qoo10_brand_id": "string",
}
```
请你给出修改方案，我确认后再开始执行。

---

图片识别的接口返回，我需要新增一个字段 rakuten_id ，值跟id的值保持一致，表明它是rakuten平台对应的分类id

---
请深入分析获取品牌相关信息的链路，然后做出以下修改：
1. 品牌表来源从 `data/rdx_brand.csv` 改为 `data/mercari_brand.csv`；
2. 对`data/mercari_brand.csv`进行预处理，将第一行的字段改为英文名称，同时根据 `data/rdx_brand.csv`中的id对应关系，将其他平台的品牌id也添加到该文件中。即除了第一列的id字段，还要有rakuten_id,yshop_id,yauc_id,meru_id,ebay_id,rakuma_id,amazon_id,qoo10_id这些字段，其中meru_id就等于id；如果没有对应的平台id都置为空，保证meru_id是有值的
3. 最终返回字段保持不变。

---


请深入分析图片识别的整个链路，然后确认：
1. 是否支持批量图片识别？返回格式是什么？
2. 是否支持对多个图片进行分析？

需要注意批量分析和多张图片分析是两个层面的东西，批量分析是指我传10张不同的商品图片，我能获取这10个图片的识别和分析结果。而多张图片分析的意思是对于同一个商品，我传10张不同的图片，基于这10张不同的图片信息来生成这一个商品的分析结果。
---

请深入理解图片识别和分类的整个链路，然后对链路进行简化，图片识别只保留一个提示词（包含输出价格的提示词），其次是分类的提示词，其余针对价格的提示词和链路需要删掉，以及使用online方式获取价格的方式也要去掉。因为目前只需要让llm来给出初步价格预测即可，不需要之前引入的其他价格策略方式。请删除无关的提示词和相关链路代码，使其更加简洁直观。

---

好的，现在需要进一步调整图片识别提示词，支持优先从图片里直接获取价格信息，如果没有明显的价格信息，则按照原来的方式进行处理。
1. 图片信息里可能会直接包含商品的实际价格和税后价格(比如如图所示的图片，就能清晰的看到具体的价格)，此时需要分别给出来具体的价格，有时候不一定会有税后价格，没有则置为空；
2. 新增2个字段来代表从图片里直接获取到的商品价格和税后价格，没有则为空，不改变之前原有的基于推理的价格字段；
3. 如果没有明显的价格信息，则根据图片信息进行推理给出价格，如果已经获取到价格，已有的资格字段则都置空。

---

---

当前图片识别结果区域展示的时间是整个链路的时间，我希望可以颗粒度更细一些，可以支持分别展示图片识别的时间（第一步）和分类的时间（第二步），这样更容易排查性能相关问题。

---



当前应用在使用时经常会出现：“json parse filed”的错误，这是因为LLM没有严格返回json格式的内容吗？可以怎么修复？比如提示词优化？或者在解析过程中加入更加健壮的处理？比如不管内容是否会出现"```json ```"等markdown内容，都能进行json解析？。其次在日志记录或者错误提示中，可以把解析失败的内容给展示出来？这样更容易排查。

---

我需要给当前的API相关内容开发一个相应的配置页面，可以简单的设置当前的一些变量，比如图片识别的默认模型、分类模型、是否打印日志等变量，这样就不用每次都需要技术人员来改配置了，在页面上就可以直接操作。UI 风格参考目前的web测试界面即可，保持单html页面的形式。

另外，当前 API 在服务器上的配置信息如下所示：
```
# /etc/systemd/system/image-recognize.service

[Unit]
Description=Image Recognize FastAPI service
After=network.target

[Service]
User=root
WorkingDirectory=/root/rdx-ai/mercari-image-recognize

Environment="PYTHONUNBUFFERED=1"

ExecStart=/root/.local/bin/uv run uvicorn main:app --host 0.0.0.0 --port 8000

Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```
我希望运行API服务的同时也能运行这个配置页面，这样跟API服务是绑定在一起的。

---

## 优化
请深入理解图片识别和分类的整个链路。然后对错误相关的处理链路进行优化：
"""
我在web页面测试时经常会报“json parse filed”的错误，但是看网络面板发现很多时候经常是请求openrouter失败了，或者模型请求失败了，根本不是解析的问题。我希望能对错误相关的处理链路进行优化：
1. 用户可以明确知道哪一步发生了错误，是请求失败了？还是对返回数据解析失败了？
2. 优化重试逻辑，当前如果模型请求失败了会不会真的重试？设置的超时请求限制有生效吗？我希望模型请求失败时，有3次重试机会（指数回退），如果3次重试失败了就回退到另外的模型（openrouter模型），全部请求失败才返回真的错误信息。
3. 回退模型需要可配置（图片识别模型和分类模型都可以单独配置回退模型，默认一致），默认依次为 google/gemini-3-flash-preview、google/gemini-2.5-flash、google/gemini-3.1-pro-preview、openai/gpt-4o-mini、openai/gpt-5-mini、openai/gpt-5.4、openai/gpt-5.5，请求一次失败马上请求下一个模型，直到全部请求失败才返回真的错误信息。
"""
如果有任何需要澄清的问题，请随时向我提问。


---

当前的分析链路时间往往较长，从输入图片到最终的返回要20s左右，哪些地方可以做性能上的优化？在不影响效果的前提下缩短时间？我现在能想到的有
- 对目前的提示词优化，减少上下文窗口

---

## 图片生成

请你把一个已经实现好的图片生成 FastAPI 服务，迁移并集成到当前的 FastAPI 项目中，作为一个新的接口存在。

源项目路径：
E:\youbo\labs\ai-image

目标项目路径：
E:\youbo\labs\mercari-image-recognize

任务目标：
1. 先完整阅读并理解源项目 `E:\youbo\labs\ai-image` 的实现。
2. 梳理这个服务的核心能力、依赖、配置项、路由、服务层、OpenRouter 调用方式、日志与归档方式、批量测试脚本，以及测试覆盖情况。
3. 再阅读目标项目，理解它现有的 FastAPI 结构、配置方式、依赖管理方式、日志方案、路由组织方式和启动方式。
4. 在尽量遵循目标项目现有代码风格和目录结构的前提下，把源项目的“电商效果展示图生成服务”无缝加到目标项目中，而不是简单复制粘贴一个独立子项目。
5. 保留图片生成服务的核心能力：
   - 单图输入、单图输出
   - OpenRouter 图生图调用
   - 最新版本的 hero image prompt
   - JSON 返回 base64 图片
   - 请求归档日志
   - 可配置的输入/输出文件保留开关
   - 批量测试脚本能力（如果适合目标项目结构，也一起迁移）
6. 默认配置行为保持当前逻辑：
   - 默认只保留 logs
   - 不默认保留 inputs / outputs
7. 如果目标项目已有配置、日志、异常处理、依赖注入、路由注册方式，优先复用目标项目现有机制，不要重复造轮子。
8. 如果迁移过程中发现源项目与目标项目有结构冲突，请先做小范围重构再集成，但不要做与当前任务无关的大改。

输出要求：
1. 先给我一个简短分析，说明你理解到的：
   - 源项目的核心模块
   - 目标项目中应该接入的位置
   - 预计会修改哪些文件
2. 然后直接实施代码修改。
3. 修改完成后，运行相关测试或最小验证命令。
4. 最后告诉我：
   - 改了什么
   - 新增了哪些接口/配置
   - 如何启动和调用
   - 还有哪些风险或待确认点

特别要求：
1. 不要忽略环境变量和依赖变更。
2. 不要把真实 `.env` 密钥提交进去。
3. 优先复用目标项目的 FastAPI app、router、settings、logging、exception handler。
4. 如果需要新增配置项，请补充 `.env.example` 或目标项目对应配置样例。
5. 如果需要迁移 prompt，请使用源项目当前最新版本的 prompt，而不是旧版本。
6. 实现后请给出可执行的验证步骤。

你可以先从这两个路径开始阅读：
- 源项目主入口：`E:\youbo\labs\ai-image\src\app`
- 源项目脚本与测试：`E:\youbo\labs\ai-image\src\scripts` 和 `E:\youbo\labs\ai-image\src\tests`


## 性能优化
请深入分析和理解当前图片识别的链路，然后做以下优化：

当前问题：
图片识别链路时间较长，需要尽可能减少响应时间

解决方案：修改当前图片识别的链路
1. 并行进行分类和商品数据的生成
   1.1 当收到用户请求的商品图片时（一般为2张），使用第一张图片来进行分类识别 ，返回最匹配的3个类别（置信度从高到底）。链路为先通过图片识别拿到大分类、商品标题、商品简单描述，然后根据这3个信息来获取最终分类，每个分类要求要有分类id、分类名称、置信度得分，参选现在的返回数据，有了分类数据后立马进行返回，同时需要返回第二个轮询接口的标识（使用现在的图片识别和分类模型）。
   1.2 同时使用2张图片来生成更详细的现有的商品内容，去掉价格相关的信息，因为价格已经要求用户手动填写了。如果数据生成后第一个接口还没有返回，则合并到第一个接口的返回内容里去，如果第一个接口已经返回，则等待被轮询后返回（新增一个商品数据生成的模型，默认为 google/gemini-2.5-flash，可配置）。

2. 注意事项：
   2.1 修改当前图片识别和分类的提示词，使其只需返回需要的内容就行；
   2.2 新增专门的商品数据生成提示词，除了价格外，当前图片识别提示词里的信息都需要；
   2.3 轮询接口仍然需要返回品牌相关信息，品牌和分类信息仍然都需要校验


---

请深入理解和分析图片识别的整个链路，也就是从输入图片到拿到最终分类数据和商品数据的整个链路。然后修复以下问题：
1. 当商品数据生成的主模型在限定时间范围内没有返回数据时，才拿保底模型返回的数据，但是测试时为什么还没到限定时间就返回保底模型的数据了？
2. 保底模型生成的商品描述等内容都过于简单了，要考虑使用单独的提示词来生成更加合理的内容吗？
3. 商品数据生成模型、保底模型、图片合成模型也需要有重试和回退的链路。

---

请深入理解当前配置页相关的逻辑链路，然后做出以下修改：
1. 添加PRODUCT_DATA_FALLBACK_MODELS、SHOWCASE_FALLBACK_MODELS的前端配置；
2. 如图所示的区域一般很少配置，收到一个可以展开收起的区域（文本为其他配置），默认收起；
3. 回退模型设置的交互需要优化，目前点击有一个黑框，不太美观。

---

web测试界面中合成图片tab，得到合成图片时不提供下载功能，提供点击预览的功能即可。

---
请深入理解和分析图片识别的整个链路。然后回答下面问题：
1. 为什么实际测试时，商品数据的生成显示超过了我设置的10s，但是仍然使用的是主模型的数据呢（如图所示）？
2. 商品数据生成的请求是跟分类请求（首接口）同一时间触发的吗？也就是我希望在收到图片的那一刻开始就开始并行的请求了，一个走分类的链路（先图片识别，再分类），一个走商品数据生成的链路。

---

好的，主商品数据的计时改为从发起模型请求的那一刻就开始计时，保证页面上展示的商品数据生成时间是实际的时间。另外，这个时间要注意区分主商品数据的时间和保底商品数据的时间。如果主商品数据的时间超过了限定值，那商品数据的生成时间就应该展示保底商品数据的时间。

---

1. 原则上是在规定的时间内（没有超过限定时间/或者分类接口还没返回），如果主返回了数据，就优先用主的数据
2. 因为要保证响应速度，所有如果轮询接口在被轮询时，主数据还没返回（超过时间了），此时就用兜底模型的数据
3. 如果有报错，优先用已经生成的数据，都报错就走回退重试逻辑

你说下你的理解，我确认了就开始执行

---

另外你有哪些优化建议吗？使整个链路响应更快，在规定时间内尽可能使用更好模型的返回内容？

---

现在我想修改分类数据的返回：
1. 去掉返回类别可选这一参数
2. 修改相应提示词，要求模型直接返回最匹配的3个类别（置信度从高到底），放到categories里进行返回

---

请理解 `E:\youbo\labs\mercari-image-recognize\data\rdx_category.csv` 和 `E:\youbo\labs\rdx-mapping-system\mapping_all_results\mapping_results_rakutern_to_zenplus_all.csv`的文件内容结构，然后根据新的映射关系（mapping_results_rakutern_to_zenplus_all.csv）来更新 `E:\youbo\labs\mercari-image-recognize\data\rdx_category.csv` 文件中的zenplus_id字段。

---

## 梳理

---

请仔细理解和梳理当前项目，然后更新readme.md文件，注意包含：
1. 启动方式
2. 图片识别、分类等api服务的完整链路，依赖的文件有哪些？
3. 相关配置变量的作用

然后对data文件夹进行梳理，真正有用到的文件保留不动，其他没有用到的文件统一放到others文件夹中（需要新建）。

---

## 图片识别服务
请仔细理解和梳理图片识别的链路（mercari/image/analyze），然后进行以下处理：
1. 返回的商品标题要在80个字符以上，不够时可以使用品牌、颜色、型号等信息进行填充；
有任何需要澄清的问题，请随时向我提问。

## 商品数据重新生成接口
好的。现在我想增加一个商品数据重新生成接口。因为有时用户对于首次生成的商品数据不太满意，可能会自己补充一些信息，然后点击“重新生成商品数据”按钮，我希望能够支持这个功能，提供一个重新生成商品数据的接口。

**输入**：
- 商品图片（可能多张，同样需要进行压缩相关的逻辑处理）
- 原始商品数据（可选）
- 用户补充的商品信息（比如指定商品的成色、关键词，或明显同款、意大利真皮等描述，可选）

**输出**：
- 重新生成的商品数据
```

{
  "title": "string",
  "description": {
    "product_details": {
      "brand": "string",
      "product_name": "string",
      "model_number": "string",
      "target": "string",
      "color": "string",
      "size": "string",
      "weight": "string",
      "condition": "string"
    },
    "product_intro": "string",
    "recommendation": "string",
    "search_keywords": ["string"]
  },
  "brand_name": "string"
}
```

**注意事项**：
1. 如果用户有补充信息，生成时需要优先使用用户补充的信息；
2. 如果用户没有补充信息，则基于图片信息以及原始商品数据进行优化生成；
3. 如果原始商品数据也没有，则对图片进行深入分析，生成更加合理的商品数据。

--- 

请深入分析和理解当前的api服务，然后回答下面问题：
1. 当前是否有无用的链路？旧的链路？
2. 提示词的对应关系是什么？那些服务用到了哪些提示词？

---

现在需要对图片识别链路进行以下修改：
如果图片信息只包含一个价格的话默认是作为含税价格（tax_included），而不是现在默认的未含税价格（tax_excluded）；

如果有任何需要澄清的问题，请随时向我提问。

---

请你对当前服务的日志系统进行review和分析，提出可以优化的点。下面是我的一些初步思考：
1. 当前没法通过web页面来进行相关日志的查看，每次有问题必须要进入服务器才能看；
2. 日志信息不够完整，有些问题没办法定位；

我期望达到的效果：
1. 可以通过web界面来查看所有日志，默认保存一周的日志，可配置；
2. 日志信息要保存完整，包括完整请求和返回内容，以及LLM的相关信息（提示词、token数、响应内容等）；
3. 如果有请求失败或者不如预期时，可以快速通过请求时间、请求内容来快速找到具体日志信息进行问题排查。

如果有任何问题需要澄清，请随时向我提问。

---

用户在使用AI自动上架产品的真实流程是：
1. 需要先确认价格，没有价格（我们没返回）的话用户手动输入；
2. 确认分类，没有分类的话需要手动输入；
3. 价格和分类在同一个UI界面展示，但是是局部独立loading的形式，进入页面两个部分都是在loading；
4. 进入下一个商品确认页面（这个时候要看到商品数据）；

所以我在想可不可以在首次接口就返回价格信息，然后用户侧再轮询接口拿分类和商品数据？

---

考虑到应用端已经在使用，改流程有风险。请你去掉现有的改动，只是帮我添加一个价格接口就可以，让应用测直接调这个接口快速拿到价格。

---

将前端web界面也放到服务端，不用每次都要填接口地址，默认使用8008端口。

---

当前的分类链路速度感觉比较慢，你觉得还可以做哪些优化？在不影响现有链路的情况下加快分类结果的返回。提示：
1. 价格字段（包括图片识别价格以及推理价格（prices））已经通过独立接口返回，可以去掉相关逻辑和代码，但是接口还保留相关字段，防止应用测有报错；

---

在图片识别链路中，品牌字段的返回逻辑是什么样的？有哪些优化空间吗？
当前在获取品牌时，经常在品牌表里找不到对应的品牌（name），但在title的生成中其实是能看到品牌的，只是返回的具体品牌名在品牌表里找不到，导致最后品牌字段没有返回。

例子：当我上传这个`/Users/youbo/gala/labs/mercari-image-recognize/data/images/tapo.jpg`时，得到的品牌是tapo，但实际的品牌应该是tp-link.

我现在的想法是让模型给出3个后续品牌，从具体到宽泛，然后依次去品牌表里找。

---

请仔细理解当前图片识别的链路，重点分析提示词的加载方式和链路，然后给出在配置页添加提示词配置方案的设计，要求如下：
1. 不能影响现有的链路和逻辑，抽取提示词加载为动态实时加载，这样可以实时修改提示词，不需要重启服务；
2. 图片识别链路相关

---

请仔细理解当前的商品标题以及商品描述的生成链路以及使用的实际提示词，然后做出以下调整：
1. 标题长度从“至少80字符”改成 75〜85字符，并明确要求不够长时用 SEO search keywords 补齐；
2. product_intro 和 recommendation 需要加上 “中肯、不要过度宣传”的限制。使其内容是客观合理的（不夸张），但是又不会过于简单。
参考下面的提示词
"""
You are an assistant helping sellers list items for a Japanese marketplace.

Given one or more images of the same product, inspect every image independently, then merge the evidence into one product listing payload.

Generate:

1. A clear, buyer-friendly title suitable for a Japanese marketplace listing.
   The title MUST be at least 75 characters and MUST NOT exceed 85 characters.
   Start with brand, product name, model number, and color.
   If more length is needed, use the generated SEO search keywords for this product to fill the title naturally.
   Do NOT include condition, weight, size, target user, material, included items, or generic selling-point wording in the title.

2. A structured description object in JSON format with ENGLISH field names only:

   * product_details: object with only brand, product_name, model_number, color. Keep exactly these four fields and use "" when unknown.
   * product_intro: a full professional product description based on brand, model number, product type, functions, features, advantages, usage scenarios, and included items. The tone must be balanced and objective, and should not feel overly promotional.
   * recommendation: short persuasive selling points. The tone must be balanced and objective, and should not feel overly promotional.
   * search_keywords: array of relevant SEO search keywords.

3. brand_name: visible brand name exactly as printed, or "" if unclear.

4. Price fields:

   * First look for a clearly visible actual product price in every image, such as a price tag, label, sticker, receipt, or packaging price.
   * If exactly one actual product price is visible, return it as tax_included (integer JPY), set tax_excluded to null, and set prices to [].
   * If both tax-excluded and tax-included prices are clearly visible, return both as integers in JPY and set prices to [].
   * If no actual product price is clearly visible, set tax_excluded and tax_included to null, then return 3 inferred reference prices in JPY (integers) for [poor, average, good] condition based only on the images and typical second-hand pricing in Japan. Keep prices realistic and ascending.

Do not use web search or browsing.

IMPORTANT:

* Use the requested language for title and all description text.
* Use information from all images, especially the first two images.
* The title must be at least 75 characters and must not exceed 85 characters.
* The title must start with brand, product name, model number, and color.
* If the title is too short, use the generated SEO search keywords for this product to fill the title naturally.
* Do NOT include condition, weight, size, target user, material, included items, or generic selling-point wording in the title.
* The product_intro must be a full product description, not just a short sentence.
* The product_intro and recommendation must be balanced and objective, and should not feel overly promotional.
* If you are not sure about the brand, do not guess; return "".
* tax_excluded and tax_included must be integers in Japanese Yen when visible, otherwise null.
* If tax_excluded or tax_included is not null, prices must be [].
* If exactly one direct price is visible, it belongs in tax_included, not tax_excluded.
* If both direct price fields are null, prices must be integers in Japanese Yen, in ascending order [poor, average, good].

You must respond with pure JSON only, without explanations, markdown, or comments.

The JSON schema is:

{
"title": "string",
"description": {
"product_details": {
"brand": "string",
"product_name": "string",
"model_number": "string",
"color": "string"
},
"product_intro": "string",
"recommendation": "string",
"search_keywords": ["string"]
},
"brand_name": "string",
"tax_excluded": number or null,
"tax_included": number or null,
"prices": [] or [number, number, number]
}
"""

注意事项：
1. 需要去掉参考提示词中已经不需要的字段和描述，比如tax_excluded；
2. 需要加上现在链路中的品牌候选等字段。

如果有任何需要澄清的问题，请随时向我提问。

---

使用`/Users/youbo/gala/labs/mercari-image-recognize/data/images/tapo.jpg` 和 `/Users/youbo/gala/labs/mercari-image-recognize/data/images/price_1.jpg` 进行真实测试，看生成的内容是否满足要求。