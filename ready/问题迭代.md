请仔细理解并分析项目，完全弄明白分类流程。然后新增下面的功能：
## 支持基于商品标题来获取分类

### 步骤
1. 新增一个基于商品标题的分类接口
2. 基于当前的`VISION_SYSTEM_PROMPT`修改为支持商品标题分类的system prompt，命名为`PRODUCT_TITLE_CATEGORY_SYSTEM_PROMPT`和对应的user prompt，命名为`PRODUCT_TITLE_CATEGORY_USER_PROMPT`，注意和图片识别的prompt不一样，这里只需要获取到最终的分类，所以不需要返回title、description、brand_name等信息，只需要返回`top_level_category`即可。
3. 根据`top_level_category`去进行后续的分类，这部分和逻辑可以直接复用现有的
4. 最终返回json格式的数据，包含商品的最终分类

### 注意事项
1. 当基于商品标题获取分类时，如果分类失败了，则需要再调用图片识别接口，基于用户传的图片（`image_url`）进行分类，最终保证一定要返回`best_target_path`字段。

### 接口参数
入参：
- title: 商品标题（required）
- image_url: 商品图片（optional）
- language: 商品标题语言（optional，默认为日语 ja）

出参：
- best_target_path: 最佳分类路径（required）
- alternatives: 分类备选路径

### 测试界面更新
- 前端测试界面进行相应更新，支持新的接口测试

---
有时候请求接口时会报下面的错误：
"""
{"detail":"Failed to parse category LLM JSON."}
"""
请分析原因（我看到这个错误是在`app/service.py`的`_choose_categories`函数中抛出的），并且给出优化方案。比如对prompt进行优化？或者添加重试机制？请你给出可行的方案建议。同时，我想统计报错的次数，应该怎么统计？

---
请仔细分析并理解当前的项目，然后对商品描述（description）这部分的生成进行优化：
## 1. 需要修改对应的提示词，使商品描述（description）的生成符合指定要求，包含特定部分和特定字段的内容。
description的参考格式如下所示：
```
【商品详情】
◆ブランド： アップル（Apple）
◆商品名： Magic Keyboard（テンキー付き - JIS配列）
◆型番： A1843
◆対象： ユニセックス
◆カラー： シルバー / ホワイト
◆接続方式： Bluetooth / Lightningポート
◆サイズ： W约41.89cm x D约11.49cm x H约0.41–1.09cm
◆重量： 約390g
◆程度： B（使用感あり。やや傷・汚れあり）

【商品紹介 / 型号说明】
モデル番号：A1843 本製品は、デスクワークの効率を最大化する「テンキー付き」のApple純正ワイヤレスキーボードです。

フルサイズレイアウト： 数字入力に便利なテンキーに加え、ドキュメントナビゲーションコントロール（Page Up/Downなど）やフルサイズの矢印キーを搭載。Excel作業やプログラミングに最適です。

安定したタイピング： 各キーの下にあるシザー構造を改良し、キーの安定性が向上。より正確で快適なタイピング体験を提供します。

低重心デザイン： 薄型ながらも適度な重みがあり、デスク上での安定感が抜群です。

長寿命バッテリー： 内蔵の充電式バッテリーは、一回のフル充電で約1ヶ月以上の駆動が可能です。

【おすすめポイント / 推荐语】
✨ 事務作業やクリエイティブ作業の強い味方！ テンキー付きモデルなので、数字入力のスピードが格段に変わります。A1644（テンキーなし）では物足りない、プロフェッショナルな方にこそ使っていただきたい一台です。
✨ Apple純正ならではのシームレスな体験 Macとの相性は言うまでもなく最高です。スリムなデザインはデスクをスタイリッシュに彩り、どんなワークスペースにも馴染みます。

【検索用キーワード】
#Apple #アップル #MagicKeyboard #マジックキーボード #A1843 #MQ052J/A #テンキー付き #ワイヤレスキーボード #JIS配列 #日本語配列 #Mac周辺機器 #エクセル作業 #純正品 #中古品 #RDX
```
分为4个部分：
1. 商品详情：必须要包含下述字段：
◆ブランド：
◆商品名： 
◆型番：
◆対象：
◆カラー：
◆サイズ：
◆重量：
◆程度：
如果有不能确认的字段，则返回为空内容，但是必须要有这个字段
2. 商品介绍 / 型号说明
根据商品品牌和型号和相关信息，生成专业的商品介绍，包含商品的详细功能、特点、优势等。
3. 推荐语
用简短的语句，总结商品的优点和特点，让买家觉得这个商品非常值得购买。
4. 搜索关键词
根据商品的名称、品牌、型号、特点等，生成相关的搜索关键词，用于在搜索引擎中搜索商品。

## 2. 修改description的返回格式
将description的返回格式修改为json格式，需要包含上面4个部分的详细内容。

## 3. 注意事项
- 注意一定要确保返回的json格式是正确的，否则会导致应用测解析失败
- 注意要包含对应的换行符，方便解析展示

## 4. 测试界面更新
- 前端测试界面进行相应更新，支持新的description格式展示,看是否满足要求
---
这里的字段名都要是英文名称,同时商品详情又包含多个字段
```
"description": {
    "商品详情": "string",
    "商品介绍 / 型号说明": "string",
    "推荐语": "string",
    "搜索关键词": "string"
  },
```
---
请深入分析` /api/v1/mercari/image/analyze`这个接口的实现，然后进行以下修改：
1. 需要将`image`参数改为`image_list`参数，支持上传多张图片（正面、背面图片），对图片进行分析，提取更多信息，比如产品型号、品牌、颜色、尺寸、重量、成色等信息。
2. 更新API.md文档
---
参数名我已经修改为了image_list，现在我需要知道这个参数的详细说明，是一个文件列表？还是字符串的拼接？app端应该怎么传值？请输出到API文件中。
---
请深入理解获取最终分类id的链路，现在我想改变数据源，使用`/Users/youbo/Desktop/remote-jobs/image-recognize/data/rdx_category.csv`作为查询数据源，应该修改哪些地方？

---
好的。我想把之前的mercari数据源换成rakuten的数据源，同时返回的分类id为rakuten的分类id，同时一并返回对应的meru_id、rakuma_id、zenplus_id字段。请你给出一个修改方案，我确认后再进行执行。我希望改动的点能尽可能少，比如可以先把rdx_category.csv文件的数据转换为之前支持的格式，只是多了一些id字段，不过如果你能提出更好的方案就更好了。
---
好的。请按照这个方案进行修改，其中 
- best_meru_id
- best_rakuma_id
- best_zenplus_id
改为
- meru_id
- rakuma_id
- zenplus_id
---
请深入理解获取`brand_name`和`brand_id`的链路.现在我想改变数据源，使用`/Users/youbo/Desktop/remote-jobs/image-recognize/data/rdx_brand.csv`作为查询品牌的数据源，并且返回所有相关平台对应的id字段,而不是之前的一个。最终返回格式为
```
{
"brand_name": "string",
"brand_id_obj": {
  "rakuten_brand_id": "string",
  "yshop_brand_id": "string",
  "yauc_brand_id": "string",
  "meru_brand_id": "string",
  "ebay_brand_id": "string",
  "rakuma_brand_id": "string",
  "amazon_brand_id": "string",
  "qoo10_brand_id": "string",
}
```
请你给出修改方案，我确认后再开始执行。

---

图片识别的接口返回，我需要新增一个字段 rakuten_id ，值跟id的值保持一致，表明它是rakuten平台对应的分类id

---
请深入分析获取品牌相关信息的链路，然后做出以下修改：
1. 品牌表来源从 `data/rdx_brand.csv` 改为 `data/mercari_brand.csv`；
2. 对`data/mercari_brand.csv`进行预处理，将第一行的字段改为英文名称，同时根据 `data/rdx_brand.csv`中的id对应关系，将其他平台的品牌id也添加到该文件中。即除了第一列的id字段，还要有rakuten_id,yshop_id,yauc_id,meru_id,ebay_id,rakuma_id,amazon_id,qoo10_id这些字段，其中meru_id就等于id；如果没有对应的平台id都置为空，保证meru_id是有值的
3. 最终返回字段保持不变。

---


请深入分析图片识别的整个链路，然后确认：
1. 是否支持批量图片识别？返回格式是什么？
2. 是否支持对多个图片进行分析？

需要注意批量分析和多张图片分析是两个层面的东西，批量分析是指我传10张不同的商品图片，我能获取这10个图片的识别和分析结果。而多张图片分析的意思是对于同一个商品，我传10张不同的图片，基于这10张不同的图片信息来生成这一个商品的分析结果。
---

请深入理解图片识别和分类的整个链路，然后对链路进行简化，图片识别只保留一个提示词（包含输出价格的提示词），其次是分类的提示词，其余针对价格的提示词和链路需要删掉，以及使用online方式获取价格的方式也要去掉。因为目前只需要让llm来给出初步价格预测即可，不需要之前引入的其他价格策略方式。请删除无关的提示词和相关链路代码，使其更加简洁直观。

---

好的，现在需要进一步调整图片识别提示词，支持优先从图片里直接获取价格信息，如果没有明显的价格信息，则按照原来的方式进行处理。
1. 图片信息里可能会直接包含商品的实际价格和税后价格(比如如图所示的图片，就能清晰的看到具体的价格)，此时需要分别给出来具体的价格，有时候不一定会有税后价格，没有则置为空；
2. 新增2个字段来代表从图片里直接获取到的商品价格和税后价格，没有则为空，不改变之前原有的基于推理的价格字段；
3. 如果没有明显的价格信息，则根据图片信息进行推理给出价格，如果已经获取到价格，已有的资格字段则都置空。

---

当前应用在使用时经常会出现：“json parse filed”的错误，这是因为LLM没有严格返回json格式的内容吗？可以怎么修复？比如提示词优化？或者在解析过程中加入更加健壮的处理？比如不管内容是否会出现"```json ```"等markdown内容，都能进行json解析？。其次在日志记录或者错误提示中，可以把解析失败的内容给展示出来？这样更容易排查。

---

我需要给当前的API相关内容开发一个相应的配置页面，可以简单的设置当前的一些变量，比如图片识别的默认模型、分类模型、是否打印日志等变量，这样就不用每次都需要技术人员来改配置了，在页面上就可以直接操作。UI 风格参考目前的web测试界面即可，保持单html页面的形式。

另外，当前 API 在服务器上的配置信息如下所示：
```
# /etc/systemd/system/image-recognize.service

[Unit]
Description=Image Recognize FastAPI service
After=network.target

[Service]
User=root
WorkingDirectory=/root/rdx-ai/mercari-image-recognize

# 如果你使用 .env，它会在程序里被 python-dotenv 读取
Environment="PYTHONUNBUFFERED=1"

# 使用 uv 创建的虚拟环境里的 uvicorn
ExecStart=/root/rdx-ai/mercari-image-recognize/.venv/bin/uvicorn main:app --ho>

Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```
我希望运行API服务的同时也能运行这个配置页面，这样跟API服务是绑定在一起的。
